import createSnapshotTest from 'test-utils/createSnapshotTest';

import PressLinks from '../PressLinks';

describe('PressLinks', () => {
  it('should render with no props passed', () => createSnapshotTest(<PressLinks />));
});
