// import React, { Component } from 'react';
// // import { any, bool, func, node, number, oneOf, oneOfType, string } from 'prop-types';
// import classNames from 'classnames';
// import styles from './podcasts.css';

// // TODO: Potential refactor from /podcasts page.
// Will review potential for simplying /podcasts length
// by creating a component specifically for <Podcast /> use on /podcasts.

// class Podcasts extends Component {
// static propTypes = {
//   children: PropTypes.oneOfType([
//     PropTypes.arrayOf(PropTypes.node),
//     PropTypes.element,
//     PropTypes.string,
//   ]).isRequired,
//   className: PropTypes.string,
// };
// static defaultProps = {
//   className: undefined,
// };
// render() {
//   const { props } = this;
//   return <div className={classNames(props.className, styles.podcasts)}>{props.children}</div>;
// }
// }

// export default Podcasts;
